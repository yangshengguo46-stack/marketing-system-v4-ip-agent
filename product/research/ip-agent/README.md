# IP Agent research quarantine

This directory contains non-runtime research assets retained for later review.
They are not registered as production tools, routes, repositories or middleware,
and the production Python package must not import them.

The current clean IP Agent contract is `IP_AGENT.md`. Research material here,
the cinematic Skill library and vendored third-party sources must not be
described as active product capability until a future architecture explicitly
promotes and verifies them.
