/**
 * Product chat has one customer-facing mode. Internal capabilities, including
 * skill authoring, are intentionally not selected through URL parameters.
 */
export function useSpecificChatMode() {
  return undefined;
}
